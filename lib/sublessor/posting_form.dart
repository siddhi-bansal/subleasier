import 'package:flutter/material.dart';

class SublessorForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sublessor Form'),
      ),
      body: const Center(
        child: Text(
          'Sublessor Form',
          style: TextStyle(fontSize: 24.0),
        ),
      ),
    );
  }
}